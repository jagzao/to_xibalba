# Enjambre de agentes — reglas de entrada

1. Leer `docs/00_semilla_contexto.md` + doc relevante antes de tocar código.
2. Obedecer `.claude/CLAUDE.md` (restricciones de código inmutables).
3. Nunca editar `docs/` (propiedad del humano).
4. Cada cambio de código → test GUT en `game/tests/unit/` mismo turno.
5. Antes de terminar → registrar en `.claude/memories/tech_debt.md`.
